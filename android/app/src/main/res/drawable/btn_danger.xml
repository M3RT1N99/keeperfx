<?xml version="1.0" encoding="utf-8"?>
<!--
  Destructive action. Deliberately quiet: an outline rather than a filled
  button, so it reads as "careful" without competing with Play.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <solid android:color="#00000000" />
            <stroke android:width="1dp" android:color="@color/kfx_disabled_border" />
            <corners android:radius="4dp" />
        </shape>
    </item>

    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="#33FF6A45" />
            <stroke android:width="1dp" android:color="@color/kfx_warn" />
            <corners android:radius="4dp" />
        </shape>
    </item>

    <item>
        <shape android:shape="rectangle">
            <solid android:color="#1AFF6A45" />
            <stroke android:width="1dp" android:color="@color/kfx_danger_border" />
            <corners android:radius="4dp" />
        </shape>
    </item>
</selector>
